kind: pipeline
name: build

steps:
  - name: backend
    image: golang
    commands:
      - echo "OK"

---
kind: pipeline
name: notify
clone:
  disable: true
depends_on:
  - build
steps:
  - name: notify
    image: notify:slack
    commands:
      - /usr/bin/notify
trigger:
  status:
    - success
    - failure